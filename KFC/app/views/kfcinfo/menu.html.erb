  <div class="card-footer text-muted">
    2 days ago
  </div>
</div>
<% Section.all.each do |section| %>
<%= link_to section, kfcinfo_menu_path(section: section), class:"btn btn-primary btn-lg"%>
<%end%>
<table class="table table-striped table-dark">
  <thead>
    <tr>
      <th scope="col">ID</th>
      <th scope="col">Name</th>
      <th scope="col">Price</th>
      <th scope="col">Description</th>
      <th scope="col">Image</th>
    </tr>
  </thead>
  <tbody>
    <tr>
    <% @fooditem.each do |i|%>
      <td><%= i.id %></td>
      <td><%= i.name%></td>
      <td><%= i.price%></td>
      <td><%= i.description%></td>
      <td><%=image_tag i.image.url(:thumb)%></td>
      <td><%= link_to "show", {:action=>'show', :id=>i.id}, class:"btn btn-primary btn-lg"%></td>
      <td><%= link_to "Edit", {:action=>'edit', :id=>i.id}, class:"btn btn-primary btn-lg"%></td>
    </tr>
    <%end%>
  </tbody>
</table>